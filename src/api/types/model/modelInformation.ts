export interface ModelInformation {
    id: string
    endpoint: string
    start: number
    end: number
    fromScratch: boolean
    tfjs: boolean
    oak: boolean
    map: string
    recall: string
    precision: string
}